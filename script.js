document.addEventListener('DOMContentLoaded', () => {
    // This is a general script for your website.
    // You can add more complex JS here for both pages, e.g.,
    // - Smooth scrolling for navigation links
    // - Form validation
    // - Image sliders

    console.log('Main website page loaded successfully!');

    // Example for smooth scrolling (optional, uncomment to use)
    /*
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();

            document.querySelector(this.getAttribute('href')).scrollIntoView({
                behavior: 'smooth'
            });
        });
    });
    */
});